<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.egemmerce.hc.repository.mapper.ItemSellMapper">


<!-- 상품등록 -->

<!-- 상품 전체 조회 -->
<select id="selectItemSellAll_xml_1" parameterType="int" resultType="ItemSet">

select *
from item_sell
   inner join (select ANY_VALUE(ip_no) as ip_no, ip_item_no, ANY_VALUE(ip_value) as ip_value from item_photo group by ip_item_no)tmp_table on is_item_no = tmp_table.ip_item_no
order by is_cool_price
limit #{pageNo}, 100;
</select>
<select id="selectItemSellAll_xml_2" parameterType="int" resultType="ItemSet">

select *
from item_sell
   inner join (select ANY_VALUE(ip_no) as ip_no, ip_item_no, ANY_VALUE(ip_value) as ip_value from item_photo group by ip_item_no)tmp_table on is_item_no = tmp_table.ip_item_no
order by is_cool_price desc
limit #{pageNo}, 100;
</select>
<select id="selectItemSellAll_xml_3" parameterType="int" resultType="ItemSet">

select *
from item_sell
   inner join (select ANY_VALUE(ip_no) as ip_no, ip_item_no, ANY_VALUE(ip_value) as ip_value from item_photo group by ip_item_no)tmp_table on is_item_no = tmp_table.ip_item_no
order by is_item_no desc
limit #{pageNo}, 100;
</select>


<!-- 상품삭제 -->

<!-- 상품 업데이트 -->

<!-- 상품 상세조회(아이템번호로 해당 아이템 조회) -->
</mapper>